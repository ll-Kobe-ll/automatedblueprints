---
import BaseLayout from '../layouts/BaseLayout.astro';
import { site } from '../data/site';

const title       = 'Free Lead Audit | See What Slow Response Is Costing You | Automated Blueprints';
const description = 'Answer a few quick questions and get a free, personalized video breakdown of how many jobs you are losing to slow response and a weak website, and what to do about it.';
---

<BaseLayout
  {title}
  {description}
  canonicalPath="/audit"
  bare={true}
  ogTitle="Find out how many jobs you're losing. Free, 60 seconds."
  ogDescription="Answer 11 quick questions and see what slow response and a weak website are costing you. No call, no email wall."
>

  <section class="bg-warm-50 min-h-[calc(100vh-4rem)] py-12 md:py-16" aria-label="Lead audit quiz">
    <div class="section-container max-w-2xl">

      <!-- Intro -->
      <div class="text-center mb-8">
        <p class="text-brand-600 text-sm font-semibold uppercase tracking-wider mb-3">Free Lead Audit</p>
        <h1 class="text-3xl md:text-4xl font-bold text-ink tracking-tight mb-4">
          Find Out How Many Jobs You Are Losing.
        </h1>
        <p class="text-ink-light text-lg leading-relaxed">
          Eleven quick questions. At the end you get a rough estimate, then we send a personalized video breaking down your exact numbers and what we would change.
        </p>
        <p class="text-ink-muted text-sm mt-3">Takes about 60 seconds. No cost. No obligation.</p>

        <!-- Trust line -->
        <div class="mt-6 inline-flex items-center gap-2.5 bg-white border border-warm-200 rounded-full pl-2.5 pr-4 py-1.5 shadow-sm">
          <span class="w-6 h-6 rounded-full bg-brand-50 flex items-center justify-center flex-shrink-0">
            <svg class="w-3.5 h-3.5 text-brand-600" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
            </svg>
          </span>
          <span class="text-ink-light text-xs sm:text-sm">
            Built by the team behind <a href="/pricing" class="font-semibold text-ink hover:text-brand-600 transition-colors">nine live demo sites</a> you can click through, based in Orange County.
          </span>
        </div>
      </div>

      <!-- Quiz card -->
      <div class="bg-white border border-warm-200 rounded-xl shadow-sm p-6 md:p-10">

        <!-- Progress -->
        <div id="quiz-progress-wrap" class="mb-8">
          <div class="flex items-center justify-between mb-2">
            <span id="quiz-counter" class="text-xs font-semibold text-ink-muted uppercase tracking-wider">Question 1 of 6</span>
            <span id="quiz-pct" class="text-xs font-semibold text-brand-600">0%</span>
          </div>
          <div class="h-1.5 bg-warm-100 rounded-full overflow-hidden">
            <div id="quiz-bar" class="h-full bg-brand-600 rounded-full transition-all duration-300" style="width:0%"></div>
          </div>
        </div>

        <form id="audit-form" novalidate>
          <!-- Honeypot -->
          <input type="text" name="botcheck" class="sr-only" tabindex="-1" autocomplete="off" aria-hidden="true" />

          <!-- Step 1: missed call outcome -->
          <div class="quiz-step" data-key="missed_call_outcome">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">What happens to calls you can't answer?</h2>
            <p class="text-ink-muted text-sm mb-6">Be honest, this sets the tone for the rest.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="Goes to voicemail, no follow-up">Goes to voicemail, no follow-up</button>
              <button type="button" class="quiz-option" data-label="I try to call back when I can">I try to call back when I can</button>
              <button type="button" class="quiz-option" data-label="We have a system that texts back automatically">We have a system that texts back automatically</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not sure</button>
            </div>
          </div>

          <!-- Step 2: missed calls per week -->
          <div class="quiz-step hidden" data-key="missed_calls">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">Roughly how many calls do you miss a week?</h2>
            <p class="text-ink-muted text-sm mb-6">Your best guess is fine.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="0 to 2" data-num="1">0 to 2</button>
              <button type="button" class="quiz-option" data-label="3 to 5" data-num="4">3 to 5</button>
              <button type="button" class="quiz-option" data-label="6 to 10" data-num="8">6 to 10</button>
              <button type="button" class="quiz-option" data-label="More than 10" data-num="14">More than 10</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-num="" data-unsure="1">Not sure, I do not track it</button>
            </div>
          </div>

          <!-- Step 3: response speed -->
          <div class="quiz-step hidden" data-key="response_speed">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">How fast do you reply to a new lead or form fill?</h2>
            <p class="text-ink-muted text-sm mb-6">This is the one that costs the most.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="Within 5 minutes" data-low="0.03" data-high="0.08">Within 5 minutes</button>
              <button type="button" class="quiz-option" data-label="Within an hour" data-low="0.06" data-high="0.12">Within an hour</button>
              <button type="button" class="quiz-option" data-label="A few hours" data-low="0.10" data-high="0.20">A few hours</button>
              <button type="button" class="quiz-option" data-label="Next day or whenever I can" data-low="0.15" data-high="0.30">Next day or whenever I can</button>
              <button type="button" class="quiz-option" data-label="It varies a lot" data-low="0.10" data-high="0.20" data-unsure="1">It varies a lot</button>
            </div>
          </div>

          <!-- Step 4: quote follow-up -->
          <div class="quiz-step hidden" data-key="quote_followup">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">What's your follow-up after sending a quote?</h2>
            <p class="text-ink-muted text-sm mb-6">Most businesses stop after the first try.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="I follow up automatically over time">I follow up automatically over time</button>
              <button type="button" class="quiz-option" data-label="I follow up once, maybe twice">I follow up once, maybe twice</button>
              <button type="button" class="quiz-option" data-label="I rarely follow up" data-unsure="1">I rarely follow up</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not sure</button>
            </div>
          </div>

          <!-- Step 5: quotes unanswered -->
          <div class="quiz-step hidden" data-key="quotes_unanswered">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">How many quotes went unanswered last month?</h2>
            <p class="text-ink-muted text-sm mb-6">Quotes you sent but never heard back on.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="None, I always hear back">None, I always hear back</button>
              <button type="button" class="quiz-option" data-label="A few (1 to 5)">A few (1 to 5)</button>
              <button type="button" class="quiz-option" data-label="Several (6 to 15)">Several (6 to 15)</button>
              <button type="button" class="quiz-option" data-label="More than 15">More than 15</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not sure</button>
            </div>
          </div>

          <!-- Step 6: reviews vs competitor -->
          <div class="quiz-step hidden" data-key="reviews_vs_competitor">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">Your Google review count vs. your top competitor's?</h2>
            <p class="text-ink-muted text-sm mb-6">Roughly speaking.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="We have more reviews">We have more reviews</button>
              <button type="button" class="quiz-option" data-label="About the same">About the same</button>
              <button type="button" class="quiz-option" data-label="They have more than us">They have more than us</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not sure, we don't track it</button>
            </div>
          </div>

          <!-- Step 7: asks for reviews -->
          <div class="quiz-step hidden" data-key="asks_for_reviews">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">Do you ask customers for reviews, and how?</h2>
            <p class="text-ink-muted text-sm mb-6">Pick the closest one.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="Yes, consistently after every job">Yes, consistently after every job</button>
              <button type="button" class="quiz-option" data-label="Sometimes, but not consistent">Sometimes, but not consistent</button>
              <button type="button" class="quiz-option" data-label="No, we don't ask">No, we don't ask</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not sure</button>
            </div>
          </div>

          <!-- Step 8: job source -->
          <div class="quiz-step hidden" data-key="job_source">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">Where do most jobs come from right now?</h2>
            <p class="text-ink-muted text-sm mb-6">Pick the biggest one.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="Referrals and word of mouth">Referrals and word of mouth</button>
              <button type="button" class="quiz-option" data-label="Google search">Google search</button>
              <button type="button" class="quiz-option" data-label="Facebook or social">Facebook or social</button>
              <button type="button" class="quiz-option" data-label="Paid ads">Paid ads</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not really sure</button>
            </div>
          </div>

          <!-- Step 9: average job value -->
          <div class="quiz-step hidden" data-key="job_value">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">What's an average job worth to you?</h2>
            <p class="text-ink-muted text-sm mb-6">Your best guess is fine.</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="Under $500" data-num="300">Under $500</button>
              <button type="button" class="quiz-option" data-label="$500 to $2,000" data-num="1200">$500 to $2,000</button>
              <button type="button" class="quiz-option" data-label="$2,000 to $5,000" data-num="3500">$2,000 to $5,000</button>
              <button type="button" class="quiz-option" data-label="$5,000 to $15,000" data-num="9000">$5,000 to $15,000</button>
              <button type="button" class="quiz-option" data-label="$15,000 or more" data-num="20000">$15,000 or more</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-num="" data-unsure="1">Honestly, not sure</button>
            </div>
          </div>

          <!-- Step 10: website situation -->
          <div class="quiz-step hidden" data-key="website">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">What's your website situation?</h2>
            <p class="text-ink-muted text-sm mb-6">What does a customer see when they Google you?</p>
            <div class="space-y-3">
              <button type="button" class="quiz-option" data-label="No website" data-weak="1">No website</button>
              <button type="button" class="quiz-option" data-label="DIY, I built it myself" data-weak="1">DIY, I built it myself</button>
              <button type="button" class="quiz-option" data-label="Agency-built, a professional built it">Agency-built, a professional built it</button>
              <button type="button" class="quiz-option" data-label="Not sure" data-unsure="1">Not sure</button>
            </div>
          </div>

          <!-- Step 11: contact -->
          <div class="quiz-step hidden" data-key="contact">
            <h2 class="text-xl md:text-2xl font-bold text-ink mb-2">Where should we send your breakdown?</h2>
            <p class="text-ink-muted text-sm mb-6">We record a short video on your numbers and send it here.</p>
            <div class="space-y-4">
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div>
                  <label for="cf-business" class="block text-sm font-semibold text-ink mb-1.5">Business Name <span class="text-brand-600">*</span></label>
                  <input type="text" id="cf-business" name="business" autocomplete="organization" placeholder="Smith HVAC" required class="quiz-input" />
                </div>
                <div>
                  <label for="cf-email" class="block text-sm font-semibold text-ink mb-1.5">Email <span class="text-brand-600">*</span></label>
                  <input type="email" id="cf-email" name="email" autocomplete="email" placeholder="jane@smithhvac.com" required class="quiz-input" />
                </div>
              </div>
              <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div>
                  <label for="cf-phone" class="block text-sm font-semibold text-ink mb-1.5">Phone <span class="text-brand-600">*</span></label>
                  <input type="tel" id="cf-phone" name="phone" autocomplete="tel" placeholder="(714) 555-0100" required class="quiz-input" />
                </div>
                <div>
                  <label for="cf-instagram" class="block text-sm font-semibold text-ink mb-1.5">Instagram <span class="text-ink-muted font-normal">(optional)</span></label>
                  <input type="text" id="cf-instagram" name="instagram" autocomplete="off" placeholder="@smithhvac" class="quiz-input" />
                </div>
              </div>
              <div>
                <label for="cf-website" class="block text-sm font-semibold text-ink mb-1.5">Website <span class="text-ink-muted font-normal">(if you have one)</span></label>
                <input type="url" id="cf-website" name="website_url" autocomplete="url" placeholder="https://smithhvac.com" class="quiz-input" />
              </div>
              <button type="submit" id="cf-submit" class="btn-primary-lg w-full">See My Results</button>
              <p id="cf-status" role="status" aria-live="polite" class="text-sm font-medium text-center min-h-[1.25rem]"></p>
            </div>
          </div>

          <!-- Back -->
          <div class="mt-8 pt-6 border-t border-warm-100">
            <button type="button" id="quiz-back" class="text-sm font-medium text-ink-muted hover:text-ink transition-colors hidden">
              &larr; Back
            </button>
          </div>
        </form>

        <!-- Result screen -->
        <div id="quiz-result" class="hidden text-center">
          <div id="result-icon" class="w-16 h-16 rounded-full bg-brand-50 flex items-center justify-center mx-auto mb-5">
            <svg id="result-icon-svg" class="w-8 h-8 text-brand-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <path id="result-icon-path" stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
            </svg>
          </div>
          <p id="result-badge" class="inline-block text-xs font-bold uppercase tracking-wider px-3 py-1 rounded-full mb-5">&nbsp;</p>
          <p id="result-lead" class="text-ink-light text-lg mb-2">Based on what you told me, you could be leaving roughly</p>
          <p id="result-range" class="text-4xl md:text-5xl font-bold text-brand-600 tracking-tight mb-2">$0</p>
          <p id="result-sub" class="text-ink-light text-lg mb-6">in new customer value on the table every month.</p>

          <!-- Severity meter -->
          <div class="max-w-xs mx-auto mb-8">
            <div class="flex gap-1.5 mb-2" aria-hidden="true">
              <span id="seg-1" class="h-2 flex-1 rounded-full bg-warm-200"></span>
              <span id="seg-2" class="h-2 flex-1 rounded-full bg-warm-200"></span>
              <span id="seg-3" class="h-2 flex-1 rounded-full bg-warm-200"></span>
            </div>
            <p id="meter-label" class="text-xs font-semibold text-ink-muted"></p>
          </div>

          <div id="result-notes" class="text-left max-w-md mx-auto space-y-3 mb-8"></div>

          <!-- Tiered CTA: the next step, set by severity in showEstimate() -->
          <div id="result-cta" class="result-reveal rounded-xl border p-6 md:p-7 text-left mb-6">
            <div class="flex items-start gap-4">
              <span id="result-cta-icon" class="w-11 h-11 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg id="result-cta-icon-svg" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" aria-hidden="true">
                  <path id="result-cta-icon-path" stroke-linecap="round" stroke-linejoin="round" d="" />
                </svg>
              </span>
              <div class="flex-1">
                <h3 id="result-cta-heading" class="font-bold text-ink text-lg mb-1.5"></h3>
                <p id="result-cta-body" class="text-ink-light text-sm leading-relaxed mb-5"></p>
                <div class="flex flex-col sm:flex-row gap-3">
                  <a id="result-cta-primary" class="btn-primary justify-center group" href="#">
                    <span id="result-cta-primary-label"></span>
                    <svg class="w-4 h-4 ml-1.5 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" aria-hidden="true">
                      <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
                    </svg>
                  </a>
                  <a id="result-cta-secondary" class="inline-flex items-center justify-center font-semibold text-brand-600 hover:text-brand-700 px-4 py-3 transition-colors" href="#"></a>
                </div>
              </div>
            </div>
          </div>

          <div class="result-reveal bg-warm-50 border border-warm-200 rounded-lg p-6 text-left" style="--reveal-delay: 120ms">
            <h3 class="font-semibold text-ink mb-3 flex items-center gap-2">
              <svg class="w-5 h-5 text-brand-600 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              What happens next
            </h3>
            <ol class="space-y-2.5">
              <li class="flex items-start gap-3 text-ink-light text-sm leading-relaxed">
                <span class="w-5 h-5 rounded-full bg-brand-600 text-white text-xs font-bold flex items-center justify-center flex-shrink-0 mt-0.5">1</span>
                <span>We review the exact numbers you just gave us.</span>
              </li>
              <li class="flex items-start gap-3 text-ink-light text-sm leading-relaxed">
                <span class="w-5 h-5 rounded-full bg-brand-600 text-white text-xs font-bold flex items-center justify-center flex-shrink-0 mt-0.5">2</span>
                <span>We record a short video on where your leads are leaking and what we would change.</span>
              </li>
              <li class="flex items-start gap-3 text-ink-light text-sm leading-relaxed">
                <span class="w-5 h-5 rounded-full bg-brand-600 text-white text-xs font-bold flex items-center justify-center flex-shrink-0 mt-0.5">3</span>
                <span>It lands in your inbox within 24 hours. No call required.</span>
              </li>
            </ol>
          </div>
          <p class="text-ink-muted text-xs mt-6">This is a rough estimate from the numbers you gave. Your video covers the real math.</p>
        </div>

      </div>
    </div>
  </section>

</BaseLayout>

<style>
  .quiz-option {
    @apply w-full text-left border border-warm-200 rounded-lg px-5 py-4 text-ink font-medium bg-white hover:border-brand-600 hover:bg-brand-50 focus:outline-none focus-visible:ring-2 focus-visible:ring-brand-600 transition-all;
  }
  .quiz-option.selected {
    @apply border-brand-600 bg-brand-50;
  }
  .quiz-input {
    @apply w-full border border-warm-200 rounded px-4 py-3 text-ink placeholder-ink-muted bg-white focus:outline-none focus:ring-2 focus:ring-brand-600 focus:border-transparent transition;
  }

  /* Result screen reveal — payoff should feel earned */
  #quiz-result.revealed { animation: resultFade 0.45s ease forwards; }
  @keyframes resultFade { from { opacity: 0; } to { opacity: 1; } }

  #quiz-result .result-reveal {
    opacity: 0;
    transform: translateY(16px);
  }
  #quiz-result.revealed .result-reveal {
    animation: resultIn 0.55s ease forwards;
    animation-delay: calc(var(--reveal-delay, 0ms) + 250ms);
  }
  @keyframes resultIn { to { opacity: 1; transform: translateY(0); } }

  /* Result icon gets a confident pop */
  #quiz-result.revealed #result-icon { animation: iconPop 0.5s ease 0.1s both; }
  @keyframes iconPop {
    0% { transform: scale(0.6); opacity: 0; }
    60% { transform: scale(1.08); }
    100% { transform: scale(1); opacity: 1; }
  }
</style>

<script define:vars={{ web3key: site.web3formsKey, contactEmail: site.email, bookingUrl: site.bookingUrl }}>
  const form    = document.getElementById('audit-form');
  const steps   = [...document.querySelectorAll('.quiz-step')];
  const totalQ  = 10; // question steps (excludes contact step)
  const bar     = document.getElementById('quiz-bar');
  const counter = document.getElementById('quiz-counter');
  const pct     = document.getElementById('quiz-pct');
  const backBtn = document.getElementById('quiz-back');
  const progressWrap = document.getElementById('quiz-progress-wrap');
  const result  = document.getElementById('quiz-result');
  const statusEl = document.getElementById('cf-status');
  const submitBtn = document.getElementById('cf-submit');

  const answers = {};
  let current = 0;

  function render() {
    steps.forEach((s, i) => s.classList.toggle('hidden', i !== current));
    const isContact = current === totalQ;
    const shownStep = current + 1;
    const total = steps.length;
    const percent = Math.round((current / total) * 100);
    bar.style.width = percent + '%';
    pct.textContent = percent + '%';
    counter.textContent = isContact ? 'Last step' : `Question ${shownStep} of ${totalQ}`;
    backBtn.classList.toggle('hidden', current === 0);
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }

  // Option clicks (single-select, auto-advance)
  steps.forEach((step) => {
    const key = step.dataset.key;
    step.querySelectorAll('.quiz-option').forEach((opt) => {
      opt.addEventListener('click', () => {
        step.querySelectorAll('.quiz-option').forEach((o) => o.classList.remove('selected'));
        opt.classList.add('selected');
        answers[key] = {
          label:  opt.dataset.label || opt.textContent.trim(),
          num:    opt.dataset.num,
          low:    opt.dataset.low,
          high:   opt.dataset.high,
          weak:   opt.dataset.weak === '1',
          unsure: opt.dataset.unsure === '1',
        };
        if (current < totalQ) { current++; render(); }
      });
    });
  });

  backBtn.addEventListener('click', () => { if (current > 0) { current--; render(); } });

  function fmt(n) {
    return '$' + (Math.round(n / 100) * 100).toLocaleString('en-US');
  }

  function computeEstimate() {
    const valueNum = parseFloat(answers.job_value?.num) || 2000;
    const missedNum = parseFloat(answers.missed_calls?.num) || 5;
    const low  = parseFloat(answers.response_speed?.low)  || 0.10;
    const high = parseFloat(answers.response_speed?.high) || 0.20;
    const monthlyMissed = missedNum * 4.3;
    let lo = monthlyMissed * low  * valueNum;
    let hi = monthlyMissed * high * valueNum;
    // Cap at a believable ceiling so the number never reads like a gimmick
    const CAP = 50000;
    hi = Math.min(hi, CAP);
    lo = Math.min(lo, hi * 0.6);
    return { low: lo, high: hi };
  }

  function buildNotes() {
    const notes = [];
    const anyUnsure = ['job_value', 'missed_calls', 'response_speed', 'job_source'].some((k) => answers[k]?.unsure);
    if (anyUnsure) {
      notes.push('You were not sure of some of these numbers. That is the real problem. When you cannot see your numbers, you cannot fix them. That is the first thing we set up.');
    }
    if (answers.website?.weak) {
      notes.push('Your website is not set up to catch and convert the leads you do get. The video covers what to change there too.');
    }
    const r = answers.response_speed?.label || '';
    if (r.includes('Next day') || r.includes('few hours') || r.includes('varies')) {
      notes.push('Most of this leak comes from response time. The businesses that answer first win the job, and right now you are not first.');
    } else if (r.includes('Within 5 minutes')) {
      notes.push('You respond fast, which is rare. That is a real edge. The bigger opportunity now is everything after that first reply: follow-up, no-shows, and the reviews you are not asking for.');
    }
    // Always leave them with at least one clear takeaway
    if (notes.length === 0) {
      notes.push('The number above is just the leads slipping past today. The real win is a system that catches and follows up on every one automatically, without you lifting a finger.');
    }
    return notes;
  }

  // Tier config: copy + red/amber/green visuals. Full literal class strings
  // so Tailwind's scanner keeps them in the build.
  const ICON_WARNING = 'M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z';
  const ICON_ALERT   = 'M12 9v2m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z';
  const ICON_CHECK   = 'M5 13l4 4L19 7';
  const ICON_CALENDAR = 'M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5A2.25 2.25 0 0121 11.25v7.5';
  const ICON_ROCKET   = 'M15.59 14.37a6 6 0 01-5.84 7.38v-4.8m5.84-2.58a14.98 14.98 0 006.16-12.12A14.98 14.98 0 009.631 8.41m5.96 5.96a14.926 14.926 0 01-5.841 2.58m-.119-8.54a6 6 0 00-7.381 5.84h4.8m2.581-5.84a14.927 14.927 0 00-2.58 5.84m2.699 2.7c-.103.021-.207.041-.311.06a15.09 15.09 0 01-2.448-2.448 14.9 14.9 0 01.06-.312m-2.24 2.39a4.493 4.493 0 00-1.757 4.306 4.493 4.493 0 004.306-1.758M16.5 9a1.5 1.5 0 11-3 0 1.5 1.5 0 013 0z';

  // Where the primary CTA points: a booking link if you have one, else email.
  const bookHref = bookingUrl
    ? bookingUrl
    : 'mailto:' + contactEmail + '?subject=' + encodeURIComponent('Lead Audit: let us talk');
  const bookLabel = bookingUrl ? 'Grab a time to talk' : 'Email us to get started';

  const TIERS = {
    high: {
      iconBg: 'bg-red-50', iconColor: 'text-red-600', iconPath: ICON_WARNING,
      badge: 'High Risk', badgeCls: 'bg-red-50 text-red-700',
      numCls: 'text-4xl md:text-5xl font-bold tracking-tight mb-2 text-red-600',
      seg: 3, segColor: 'bg-red-500', meter: 'Leak severity: high. This grows every month you wait.',
      lead: 'Based on what you told me, you could be leaving roughly',
      sub:  'in new customer value on the table every month. That is real money going to whoever answers first.',
      range: (est) => fmt(est.low) + ' to ' + fmt(est.high),
      cta: {
        wrap: 'result-reveal rounded-xl border p-6 md:p-7 text-left mb-6 bg-red-50 border-red-200',
        iconWrap: 'bg-red-100', iconColor: 'text-red-600', iconPath: ICON_CALENDAR,
        heading: 'This is worth fixing now, not next quarter.',
        body: 'Every week it stays open, that number walks out the door. Skip the wait and let us go through your exact setup together.',
        primaryHref: bookHref, primaryLabel: bookLabel,
        secondaryHref: '/pricing', secondaryLabel: 'Or see the Lead Flow System →',
      },
    },
    moderate: {
      iconBg: 'bg-amber-50', iconColor: 'text-amber-600', iconPath: ICON_ALERT,
      badge: 'Worth Fixing', badgeCls: 'bg-amber-50 text-amber-700',
      numCls: 'text-4xl md:text-5xl font-bold tracking-tight mb-2 text-amber-600',
      seg: 2, segColor: 'bg-amber-500', meter: 'Leak severity: moderate. Cheaper to fix now than after it grows.',
      lead: 'Based on what you told me, you are likely leaving around',
      sub:  'a month in new customer value. It adds up faster than most owners expect.',
      range: (est) => fmt(est.low) + ' to ' + fmt(est.high),
      cta: {
        wrap: 'result-reveal rounded-xl border p-6 md:p-7 text-left mb-6 bg-amber-50 border-amber-200',
        iconWrap: 'bg-amber-100', iconColor: 'text-amber-600', iconPath: ICON_CALENDAR,
        heading: 'The cheapest time to plug this is now.',
        body: 'It is a smaller leak than most, which means it is the easiest to fix before it grows. Want to walk through it together?',
        primaryHref: bookHref, primaryLabel: bookLabel,
        secondaryHref: '/pricing', secondaryLabel: 'Or see the Lead Flow System →',
      },
    },
    low: {
      iconBg: 'bg-green-50', iconColor: 'text-green-700', iconPath: ICON_CHECK,
      badge: 'Looking Good', badgeCls: 'bg-green-50 text-green-700',
      numCls: 'text-4xl md:text-5xl font-bold tracking-tight mb-2 text-green-700',
      seg: 1, segColor: 'bg-green-600', meter: 'Leak severity: low. You are ahead of most.',
      lead: 'Good news. From what you told me, your fundamentals are stronger than most.',
      sub:  (est) => 'You are likely leaving under ' + fmt(Math.max(est.high, 800)) + ' a month, which is the easy kind to plug. Your video shows how to keep it that way as you grow.',
      range: () => 'You are ahead of the pack',
      cta: {
        wrap: 'result-reveal rounded-xl border p-6 md:p-7 text-left mb-6 bg-green-50 border-green-200',
        iconWrap: 'bg-green-100', iconColor: 'text-green-700', iconPath: ICON_ROCKET,
        heading: 'Your response is solid. Let us get you found by more people.',
        body: 'You already close the leads you get. The biggest win now is a website that brings more of them in, feeding straight into your system.',
        primaryHref: '/pricing', primaryLabel: 'See the system that feeds this',
        secondaryHref: '/how-it-works', secondaryLabel: 'Or see the full system →',
      },
    },
  };

  // Tiered result: copy + severity visuals. Never shows $0.
  function showEstimate(est) {
    const t = est.high >= 10000 ? TIERS.high : est.high >= 2000 ? TIERS.moderate : TIERS.low;

    document.getElementById('result-icon').className = 'w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-5 ' + t.iconBg;
    document.getElementById('result-icon-svg').setAttribute('class', 'w-8 h-8 ' + t.iconColor);
    document.getElementById('result-icon-path').setAttribute('d', t.iconPath);

    const badgeEl = document.getElementById('result-badge');
    badgeEl.textContent = t.badge;
    badgeEl.className = 'inline-block text-xs font-bold uppercase tracking-wider px-3 py-1 rounded-full mb-5 ' + t.badgeCls;

    document.getElementById('result-lead').textContent  = t.lead;
    document.getElementById('result-range').textContent = t.range(est);
    document.getElementById('result-range').className   = t.numCls;
    document.getElementById('result-sub').textContent   = typeof t.sub === 'function' ? t.sub(est) : t.sub;

    for (let i = 1; i <= 3; i++) {
      document.getElementById('seg-' + i).className = 'h-2 flex-1 rounded-full ' + (i <= t.seg ? t.segColor : 'bg-warm-200');
    }
    document.getElementById('meter-label').textContent = t.meter;

    // Next-step CTA, tiered by severity
    const c = t.cta;
    document.getElementById('result-cta').className = c.wrap;
    document.getElementById('result-cta-icon').className = 'w-11 h-11 rounded-lg flex items-center justify-center flex-shrink-0 ' + c.iconWrap;
    document.getElementById('result-cta-icon-svg').setAttribute('class', 'w-6 h-6 ' + c.iconColor);
    document.getElementById('result-cta-icon-path').setAttribute('d', c.iconPath);
    document.getElementById('result-cta-heading').textContent = c.heading;
    document.getElementById('result-cta-body').textContent = c.body;

    const primary = document.getElementById('result-cta-primary');
    primary.href = c.primaryHref;
    document.getElementById('result-cta-primary-label').textContent = c.primaryLabel;
    if (/^https?:/.test(c.primaryHref)) { primary.target = '_blank'; primary.rel = 'noopener'; }

    const secondary = document.getElementById('result-cta-secondary');
    secondary.href = c.secondaryHref;
    secondary.textContent = c.secondaryLabel;
  }

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (form.querySelector('[name="botcheck"]').value) return;

    const business  = document.getElementById('cf-business').value.trim();
    const email     = document.getElementById('cf-email').value.trim();
    const phone     = document.getElementById('cf-phone').value.trim();
    const instagram = document.getElementById('cf-instagram').value.trim();
    const websiteUrl = document.getElementById('cf-website').value.trim();

    if (!business || !email || !phone) { setStatus('Please fill in your business name, email, and phone.', 'error'); return; }
    if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) { setStatus('Please enter a valid email address.', 'error'); return; }

    setStatus('', '');
    submitBtn.disabled = true;
    submitBtn.textContent = 'Sending...';

    const est = computeEstimate();
    const payload = {
      access_key: web3key,
      subject: 'New Lead Audit Quiz: Automated Blueprints',
      from_name: business,
      business,
      email,
      phone,
      instagram: instagram || 'Not provided',
      website_url: websiteUrl || 'Not provided',
      missed_call_outcome:    answers.missed_call_outcome?.label ?? '',
      missed_calls_weekly:    answers.missed_calls?.label ?? '',
      response_speed:         answers.response_speed?.label ?? '',
      quote_followup:         answers.quote_followup?.label ?? '',
      quotes_unanswered_last_month: answers.quotes_unanswered?.label ?? '',
      reviews_vs_competitor:  answers.reviews_vs_competitor?.label ?? '',
      asks_for_reviews:       answers.asks_for_reviews?.label ?? '',
      job_source:             answers.job_source?.label ?? '',
      job_value:               answers.job_value?.label ?? '',
      website:                answers.website?.label ?? '',
      estimated_monthly_loss: `${fmt(est.low)} to ${fmt(est.high)}`,
      botcheck: '',
    };

    try {
      const res = await fetch('https://api.web3forms.com/submit', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', Accept: 'application/json' },
        body: JSON.stringify(payload),
      });
      const data = await res.json();
      if (!data.success) throw new Error(data.message || 'failed');

      // Show result
      showEstimate(est);
      const notesEl = document.getElementById('result-notes');
      buildNotes().forEach((text) => {
        const p = document.createElement('p');
        p.className = 'flex items-start gap-2 text-ink-light text-sm leading-relaxed';
        p.innerHTML = '<span class="text-brand-600 font-bold flex-shrink-0">&bull;</span><span>' + text + '</span>';
        notesEl.appendChild(p);
      });
      form.classList.add('hidden');
      progressWrap.classList.add('hidden');
      result.classList.remove('hidden');
      // Trigger the staggered reveal on the next frame so animations run
      requestAnimationFrame(() => result.classList.add('revealed'));
      window.scrollTo({ top: 0, behavior: 'smooth' });
    } catch {
      submitBtn.disabled = false;
      submitBtn.textContent = 'See My Results';
      setStatus('Something went wrong. Email us directly at ' + contactEmail + '.', 'error');
    }
  });

  function setStatus(msg, type) {
    statusEl.textContent = msg;
    statusEl.className = 'text-sm font-medium text-center min-h-[1.25rem] ' +
      (type === 'error' ? 'text-red-600' : '');
  }

  render();
</script>
